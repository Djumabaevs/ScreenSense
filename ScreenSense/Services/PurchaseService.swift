import Foundation

// All features are free — no in-app purchases.
// Kept as a minimal stub so the Xcode project file doesn't need editing.
@Observable
final class PurchaseService {
    static let shared = PurchaseService()
}
